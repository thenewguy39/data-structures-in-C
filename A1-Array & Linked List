#include<stdio.h>
void main(){
        int arr[100],i,n;
        printf("Enter size of the array\n");
        scanf("%d",&n);
        for(i=0;i<n;i++){
                printf("Enter element\n");
                scanf("%d",arr + i);
        }
        printf("The array is :\n");
        for(i=0;i<n;i++){
                printf("Value : %d Address :%x",*(arr + i),arr + i);
        }

}
